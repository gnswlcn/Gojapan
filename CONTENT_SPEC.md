# CONTENT_SPEC.md — 콘텐츠 에이전트 기술 스펙

> 이 문서는 섀도잉 에피소드 JSON 제작 기준을 정의합니다.  
> 앱 구조 변경 시 앱 에이전트가 이 문서를 직접 수정합니다.

---

## 1. 에피소드 JSON 전체 구조

```json
{
  "episode_info": { ... },
  "vocabulary":   [ ... ],
  "dialogue_flow": [ ... ]
}
```

### 1-1. episode_info

| 필드 | 타입 | 설명 |
|---|---|---|
| `id` | string | `"ep001"` 형식, 3자리 숫자 |
| `title` | string | 한국어 제목 |
| `title_jp` | string | 일본어 제목 |
| `setting_ko` | string | 장소/상황 (한국어) |
| `setting_jp` | string | 장소/상황 (일본어) |
| `difficulty` | string | `"N5"` \| `"N4"` \| `"N3"` \| `"N2"` \| `"N1"` |
| `thumbnail` | string | 이모지 1개 |
| `description` | string | 목록 화면 표시용 한 줄 설명 |

### 1-2. vocabulary

에피소드에 등장하는 **JLPT 단어장 수록 단어** 목록.  
앱이 이 목록을 활용해 학습 완료 단어 하이라이트·XP 연동 등에 사용할 수 있음.

```json
"vocabulary": [
  { "vocab_id": "n5_002", "jp": "飲む",  "reading": "のむ",    "ko": "마시다" },
  { "vocab_id": "n5_068", "jp": "水",    "reading": "みず",    "ko": "물"     },
  { "vocab_id": "n5_082", "jp": "お金",  "reading": "おかね",  "ko": "돈"     }
]
```

- `vocab_id` 는 `/data/n5.json` ~ `/data/n1.json` 의 `id` 값과 동일
- 에피소드에 실제로 등장하는 단어만 수록 (등장하지 않으면 제외)
- `vocabulary` 배열이 비어 있어도 JSON은 유효

### 1-3. dialogue_flow

대화 턴의 배열. `speaker` 는 항상 `"clerk"`.

---

## 2. 턴 타입

### listen 턴

점원 발화를 유저가 듣고 따라 읽는 턴. 선택지 없음.

```json
{
  "id": "t01",
  "speaker": "clerk",
  "jp":       "いらっしゃいませ！",
  "jp_ruby":  "いらっしゃいませ！",
  "reading":  "いらっしゃいませ！",
  "ko_meaning": "어서 오세요!",
  "type": "listen"
}
```

### choice 턴

점원 발화 후 유저가 한국어 버튼 2개 중 하나를 선택하는 턴.  
선택지의 `jp` / `jp_ruby` / `reading` 은 **유저가 말할 일본어 응답**.

```json
{
  "id": "t02",
  "speaker": "clerk",
  "jp":       "袋はご利用ですか？",
  "jp_ruby":  "{袋|ふくろ}はご{利用|りよう}ですか？",
  "reading":  "ふくろはごりようですか？",
  "ko_meaning": "봉투는 필요하신가요?",
  "type": "choice",
  "vocab_ids": ["n5_011"],
  "choices": [
    {
      "id": "a",
      "ko":      "네, 봉투 하나 주세요.",
      "jp":      "はい、袋を一つください。",
      "jp_ruby": "はい、{袋|ふくろ}を{一|ひと}つください。",
      "reading": "はい、ふくろをひとつください。",
      "correct": false
    },
    {
      "id": "b",
      "ko":      "네, 한 장 주세요.",
      "jp":      "はい、一枚ください。",
      "jp_ruby": "はい、{一枚|いちまい}ください。",
      "reading": "はい、いちまいください。",
      "correct": true
    }
  ]
}
```

---

## 3. jp_ruby 후리가나 표기법

한자가 포함된 모든 `jp` 필드에 대응하는 `jp_ruby` 필드를 작성.

| 규칙 | 예시 |
|---|---|
| 한자+읽기: `{漢字\|よみ}` | `{袋\|ふくろ}` |
| 히라가나·가타카나·숫자: 그대로 | `いらっしゃいませ` |
| 한자 없으면 jp와 동일 | `"jp_ruby": "いらっしゃいませ！"` |

**렌더링 (앱 에이전트 담당):**  
`{漢字|よみ}` → `<ruby>漢字<rt>よみ</rt></ruby>`

---

## 4. vocab_ids 태깅 지침

### choice 턴의 점원 발화에만 태깅

`dialogue_flow[n].vocab_ids` 배열에 해당 턴 `jp` 문장에 등장하는 JLPT 단어 ID를 기재.  
listen 턴에는 `vocab_ids` 생략 가능 (앱이 무시).

### 태깅 대상

- **내용어**: 명사, 동사(기본형), 형용사(기본형), 부사
- `/data/n1.json` ~ `/data/n5.json` 에 수록된 단어만 태깅

### 태깅 제외

| 제외 항목 | 이유 |
|---|---|
| 조사 (は、が、を、に、で、へ、も、の…) | 단어장 미수록 |
| 기본 문법형태소 (です、ます、ください…) | 단어장 미수록 |
| 접속사 (そして、でも、しかし…) | 단어장 미수록 |
| 가타카나 외래어 (パスポート、カード…) | 단어장 미수록 |
| 상호명·고유명사 (コンビニ、キム様…) | 단어장 미수록 |
| 인사 관용구 (いらっしゃいませ、かしこまりました…) | 단어장 미수록 |

### 예시

```
"jp": "お支払い方法はいかがなさいますか？"
```
→ `"vocab_ids": []`  ← 支払い・方法 모두 N3 이상 단어장 확인 필요, 없으면 빈 배열

```
"jp": "袋はご利用ですか？"
```
→ `"vocab_ids": ["n5_011"]`  ← 買う(n5_011)? 아니, 利用 없음. 빈 배열로 두는 게 안전.

> 확신이 없으면 빈 배열(`[]`)로 두세요. 과태깅보다 미태깅이 낫습니다.

---

## 5. 파일 등록 방법

### Step 1 — 파일 생성

```
/data/shadow_ep###.json
```

### Step 2 — play/page.tsx 에 import 추가

```ts
// app/shadow/play/page.tsx 상단
import ep005 from '@/data/shadow_ep005.json';

// EPISODES 레지스트리
const EPISODES: Record<string, EpisodeData> = {
  ep001: ep001 as EpisodeData,
  ...
  ep005: ep005 as EpisodeData,  // ← 추가
};
```

> **주의:** Next.js static export 제약으로 동적 `require()` 불가.  
> 반드시 파일 상단에 `import` 문으로 추가해야 합니다.

### Step 3 — shadow/page.tsx 목록에 추가

```ts
// app/shadow/page.tsx
const EPISODES: EpisodeMeta[] = [
  ...
  {
    id: 'ep005',
    title: '...',
    setting_ko: '...',
    difficulty: 'N4',
    thumbnail: '...',
    description: '...',
    totalTurns: 5,   // dialogue_flow 배열 길이
  },
];
```

---

## 6. 선택지 품질 기준

| 항목 | 기준 |
|---|---|
| 정답 | 현지에서 실제로 통용되는 자연스러운 표현 |
| 오답 | 한국인이 자주 저지르는 **명확한 실수** — 직역투, 반말, 명령형, 조수사 오류, 잘못된 경어 등 |
| 두 선택지 모두 | 문맥상 어느 쪽이 맞는지 납득이 돼야 함. 둘 다 자연스러운 선택지 금지 |

---

## 7. 월드별 에피소드 주제 아이디어

### N5 — 기초 생활 (코딩명: 村)

| # | 상황 | 포인트 |
|---|---|---|
| ep001 | 편의점 기본 대화 ✅ | 봉투 枚, 데우기, 포인트카드 |
| — | 카페 주문 | ホット/アイス, サイズ |
| — | 약국에서 | 두통약, 위장약 구입 |
| — | 버스/지하철 타기 | IC카드, 환승 |

### N4 — 여행 필수 (코딩명: 街)

| # | 상황 | 포인트 |
|---|---|---|
| ep002 | 돈키호테 면세 카운터 ✅ | 免税, 조수사, お疲れ様 금지 |
| ep003 | 식당 웨이팅 취소 ✅ | キャンセル, 申します, 急用 |
| ep004 | 호텔 수건 추가 요청 ✅ | 枚 vs 個, 号室 vs 番 |
| — | 관광지 사진 부탁 | シャッターを押す |
| — | 렌터카 반납 | 傷, ガソリン満タン |
| — | 료칸 체크인 | 浴衣, 夕食の時間 |

### N3 — 생활 밀착 (코딩명: 森)

| # | 상황 | 포인트 |
|---|---|---|
| — | 병원 접수 | 保険証, 症状 설명 |
| — | 미용실 예약 | カット, パーマ, 何センチ |
| — | 부동산 문의 | 敷金, 礼金, ペット可 |
| — | 우체국 택배 | 荷物, 着払い, 速達 |

### N2 / N1 — 심화 (코딩명: 城 / 龍の巣)

| # | 상황 | 포인트 |
|---|---|---|
| — | 비즈니스 전화 응대 | お世話になっております |
| — | 은행 구좌 개설 | 印鑑, 通帳 |
| — | 민원 / 행정 | 転居届, 住民票 |
