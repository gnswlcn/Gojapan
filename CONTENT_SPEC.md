# GoJapan — 에피소드 콘텐츠 스펙

> **앱 에이전트 → 콘텐츠 에이전트**
> 이 파일에 정의된 스펙을 따르면 앱이 자동으로 에피소드를 인식합니다.
> 스펙 변경이 필요할 때는 COORDINATION.md에 요청해 주세요.

---

## 1. 에피소드 ID 규칙

```
ep001, ep002, ep003 ...  (순차 번호)
```

---

## 2. 에피소드 JSON 파일

**경로**: `data/shadow_<id>.json`  (예: `data/shadow_ep002.json`)

### 전체 구조

```jsonc
{
  "episode_info": {
    "id": "ep002",            // 에피소드 ID (위 규칙 따름)
    "world": "N5",            // "N5" | "N4" | "N3" | "N2" | "N1"
    "title": "호텔 체크인",    // 한국어 제목
    "title_jp": "ホテルのチェックイン",
    "setting_ko": "호텔 프런트",
    "setting_jp": "ホテルのフロント",
    "difficulty": "N5",
    "thumbnail": "🏨",        // 에피소드 대표 이모지
    "description": "호텔에서 체크인하기"
  },

  // 이 에피소드에서 배울 핵심 어휘 (명사/동사/형용사 위주, 조사/접속사 제외)
  "npc": {
    "name": "편의점 알바생",       // 대화 상대자 이름
    "friendly_emoji": "🙂",        // 평상시 표정
    "attack_emoji": "😤",          // 공격 시 표정
    "weapon_emoji": "🔪",          // 무기 이모지 (상황에 맞게)
    "attack_messages": [           // 랜덤 선택, 5개 권장
      "커터칼로 영수증을 두 동강 냈다!",
      "냉동 삼각김밥이 총알처럼 날아왔다!"
    ]
  },
  "vocabulary": [
    {
      "id": "ep002_v01",       // <에피소드id>_v<번호>
      "kanji": "予約",
      "reading": "よやく",
      "meaning_ko": "예약"
    }
    // 5~12개 권장
  ],

  "dialogue_flow": [
    // 턴 배열 — 아래 턴 타입 참조
  ]
}
```

---

## 3. 턴 타입

### `listen` — 점원이 말하고 유저가 따라 말하기만 함

```jsonc
{
  "id": "t01",
  "speaker": "clerk",
  "jp": "いらっしゃいませ！",
  "reading": "いらっしゃいませ！",   // 히라가나 발음
  "ko_meaning": "어서 오세요!",
  "type": "listen",
  "vocab_ids": []                   // 이 턴에서 퀴즈할 어휘 ID 목록
}
```

### `choice` — 유저가 2개 중 맞는 일본어 문장 선택 후 따라 말하기

```jsonc
{
  "id": "t02",
  "speaker": "clerk",
  "jp": "ご予約はございますか？",
  "reading": "ごよやくはございますか？",
  "ko_meaning": "예약하셨나요?",
  "type": "choice",
  "vocab_ids": ["ep002_v01"],
  "situation": "홍길동으로 예약했으니까 예약 있다고 말하자.",
  // ↑ 유저의 한국어 생각말풍선 — 어떤 상황인지 힌트 제공
  // 이 상황에 맞는 일본어를 선택지에서 고르는 방식
  "choices": [
    {
      "id": "a",
      "jp": "はい、ホン・ギルドンで予約しています。",
      "reading": "はい、ホン・ギルドンでよやくしています。",
      "correct": true               // situation과 일치하는 문장
    },
    {
      "id": "b",
      "jp": "いいえ、予約はしていません。",
      "reading": "いいえ、よやくはしていません。",
      "correct": false              // situation과 반대/틀린 문장
      // ← ko 필드 없음. 선택지는 일본어만 표시됨
    }
  ]
}
```

---

## 4. situation / 선택지 설계 원칙

- **situation**: 유저가 처한 상황을 한국어로 1~2문장. "~하자", "~해야지" 등 행동 의도 포함.
- **정답**: situation의 의도와 정확히 일치하는 일본어 문장
- **오답**: situation의 의도와 **반대**이거나 **완전히 다른** 일본어 문장 (애매한 오답 금지)
  - 나쁜 예: "봉투 필요해" 상황에서 "いいえ、大丈夫です。" (실생활엔 틀리지 않음)
  - 좋은 예: "봉투 필요해" 상황에서 "いいえ、結構です。" (필요없다는 뜻이므로 명확히 틀림)
- 선택지에 `ko` 필드 없음 — **일본어만** 표시하여 독해력 테스트

## 5. vocab_ids 작성 지침

- 해당 턴의 점원 발화 또는 선택지에 **처음 등장**하는 핵심 어휘만 태깅
- 조사(は、が、を...)·인사말·초급 문법 제외
- 한 턴에 최대 3개 권장 (유저가 대화 흐름 유지되도록)
- 이미 앞 턴에서 나온 단어는 재태깅 불필요

---

## 5. 에피소드 등록 방법

JSON 파일 작성 후 **앱 에이전트에게 요청**하거나,  
직접 `lib/episodes.ts`의 `ALL_EPISODES` 배열에 추가:

```typescript
{
  id: 'ep002',
  world: 'N5',
  title: '호텔 체크인',
  thumbnail: '🏨',
  description: '호텔에서 체크인하기',
  totalTurns: 7,   // dialogue_flow 배열 길이
}
```

그리고 `app/shadow/play/page.tsx`의 `EPISODES` 객체에 import 추가:

```typescript
import ep002 from '@/data/shadow_ep002.json';

const EPISODES: Record<string, EpisodeData> = {
  ep001: ep001 as EpisodeData,
  ep002: ep002 as EpisodeData,  // ← 추가
};
```

> **주의**: Next.js 정적 빌드 제약상 동적 import 불가 — 반드시 상단 static import 사용

---

## 6. 에피소드 설계 가이드라인

| 항목 | 권장 |
|------|------|
| 턴 수 | 5~10턴 |
| choice 비율 | 전체의 60% 이상 |
| 어휘 수 | 5~12개 |
| 첫 턴 | `listen` 타입으로 시작 (점원 인사) |
| 마지막 턴 | `listen` 타입으로 마무리 (점원 감사 인사) |
| 난이도 | world와 일치 (N5 에피소드는 N5 문법·어휘 수준) |
| NPC 무기 | 장소에 어울리게 (편의점→🔪, 호텔→🔫, 식당→🍳, 병원→💉) |
| 공격 메시지 | 5개, 과장되고 코믹하게, 장소 소품 활용 | |

---

## 7. 월드별 에피소드 주제 아이디어

| World | 주제 예시 |
|-------|-----------|
| N5 🏘️ | 편의점, 호텔 체크인, 식당 주문, 길 묻기, 쇼핑 |
| N4 🏙️ | 병원 접수, 우체국, 은행, 미용실, 대중교통 |
| N3 🏯 | 취업 면접, 집 계약, 행정 민원, 여행사 |
| N2 ⚔️ | 비즈니스 미팅, 고객 응대, 계약 협상 |
| N1 🐉 | 학술 발표, 법률 상담, 의료 전문 대화 |
